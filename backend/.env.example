# Server
NODE_ENV=development
PORT=4000
API_PREFIX=api/v1

# Database (use with docker-compose or your PostgreSQL)
DATABASE_URL="postgresql://quran:quran_secret@localhost:5432/quran_app?schema=public"

# Quran Foundation Content API (backend only; never prefix these with NEXT_PUBLIC_)
QF_CLIENT_ID=""
QF_CLIENT_SECRET=""
QF_ENV="production"
QF_AUTH_BASE_URL="https://oauth2.quran.foundation"
QF_API_BASE_URL="https://apis.quran.foundation"
AUDIO_STORAGE_PATH="storage/audio"
AUDIO_PUBLIC_BASE_URL="http://localhost:4010/api/v1/audio/files"

# Redis (use with docker-compose or your Redis)
REDIS_URL=redis://localhost:6379
CACHE_TTL_SECONDS=3600

# JWT (optional - for user features)
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d

# CORS (comma-separated origins)
CORS_ORIGINS=http://localhost:3000,https://yourdomain.com

# Rate limiting
THROTTLE_TTL=60
THROTTLE_LIMIT=100
